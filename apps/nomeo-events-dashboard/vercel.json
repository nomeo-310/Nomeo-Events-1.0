{
  "buildCommand": "pnpm store prune && pnpm install && next build",
  "installCommand": "pnpm store prune && pnpm install",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/refresh-seedphrase",
      "schedule": "0 1 * * *"
    }
  ]
}